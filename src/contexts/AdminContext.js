// Simple context definition for admin functionality
// Used by AdminProvider to provide admin operations throughout the app
import { createContext } from 'react';

export const AdminContext = createContext(null);
